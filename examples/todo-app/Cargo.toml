[package]
name = "todo-app"
version = "0.0.0"
edition = "2024"
license = "MIT"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
typewire = { version = "=0.0.2", path = "../../typewire", features = ["schemas"] }
wasm-bindgen = "0.2"
js-sys = "0.3.90"

[lints]
workspace = true
