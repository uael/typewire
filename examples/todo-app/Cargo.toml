[package]
name = "todo-app"
version = "0.0.0"
edition = "2024"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
typewire = { version = "=0.0.1", path = "../../typewire" }
wasm-bindgen = "0.2"
js-sys = "0.3.90"

[lints]
workspace = true
