[package]
name = "todo-app"
version = "0.0.0"
edition = "2024"
license = "MIT"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
typewire = { path = "../../typewire", features = ["schemas"] }
wasm-bindgen.workspace = true
js-sys.workspace = true

[lints]
workspace = true
